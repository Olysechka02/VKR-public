# 🚁 Система акустической диагностики неисправностей малых БАС

![Python](https://img.shields.io/badge/Python-3.12.10-blue.svg)
![Jupyter](https://img.shields.io/badge/Jupyter-Notebook-F37626.svg)
![Status](https://img.shields.io/badge/Status-В_разработке-orange.svg)

**Выпускная квалификационная работа (ВКР) по направлению «Техносферная безопасность».** 
Проект посвящен разработке превентивной системы обнаружения механических дефектов беспилотных авиационных систем (БАС) на основе анализа их акустических сигналов. Идея заключается в том, что акустическая подпись работающего дрона меняется до того, как дефект (например, скол пропеллера или дефект мотора) приведет к критическому отказу в полете.

---

## 🧠 Архитектура и методология

В основе проекта лежит нейросетевая архитектура **ResNet с блоками Squeeze-and-Excitation (SE)**, работающая по принципу **Multi-Task Learning (MTL)**. 

Модель решает две задачи одновременно, чтобы минимизировать влияние шума маневров на точность детекции дефектов:
1. **Главная задача (Вес функции потерь: 0.8):** Классификация состояния (Норма / Дефект пропеллера / Дефект мотора).
2. **Вспомогательная задача (Вес функции потерь: 0.2):** Определение маневра (Взлет / Зависание / Посадка).

> # 🖼️ **[Вставить сюда схему архитектуры модели MTL ResNet-SE]**

### Извлечение признаков (Feature Extraction)
Реализован комбинированный подход к извлечению признаков для повышения точности работы в условиях зашумленной среды (SNR 10-15 дБ):
* **Подход 1:** Mel-спектрограммы (оптимизированное окно: 20 кадров, перекрытие 12.5%).
* **Подход 2:** Вспомогательные признаки (STFT / FFT) для лучшего выделения высокочастотных шумов неисправного мотора.

---

## 📂 Структура проекта

Проект организован в соответствии со стандартами разработки машинного обучения:
# Обновить структуру
```text
VKR-public/
│
├── notebooks/                 # 📝 Черновики и Jupyter-тетради (тесты)
│   ├── 1model
│   ├── 2model
│   └── ...                    
│
├── src/                       # ⚙️ Исходный код проекта
│   ├── feature_extraction/    # Скрипты генерации признаков из аудио
│   │   ├── features32_STFT+FFT.ipynb     # Логика выделения Mel-спектрограмм
│   │   └── features1280.ipynb    # Логика выделения STFT/FFT признаков
│   │
│   └── training/              # Скрипты для обучения и оценки
│       ├── 01_train_model.py  # Основной пайплайн обучения MTL модели
│       └── 02_evaluate.py     # Прогон модели на всем датасете, визуализация и метрики
│
├── .gitattributes             # Настройки Git для работы с файлами
├── requirements.txt           # Зависимости и версии библиотек
└── README.md                  # Описание проекта (текущий файл)


## Инструкция по установке:
- скачайте репозиторий
- скачайте и разархивируйте датасет в папку data
- создайте виртуальное окружение
- установите библиотеки
- запустите сначала -- дождитесь пока признаки извлекуться, потом запустите --- и потом зпустите ----


